# MeetFlow 本地录制功能使用说明

## 功能介绍

MeetFlow 现已支持本地录制功能。在会议进行中，用户可以将会议的视频和音频实时录制到本地。

## 使用方法

### 开始录制

1. 加入会议后，在控制栏中找到"开始录制"按钮
2. 点击按钮，系统会：
   - 自动收集本地摄像头画面
   - 收集所有远程参与者的视频画面
   - 收集系统音频和麦克风音频
   - 开始录制，按钮会显示红色脉冲动画效果

### 停止录制

1. 点击"停止录制"按钮
2. 录制文件会自动下载到本地的默认下载文件夹
3. 文件名格式：`meeting-YYYY-MM-DDTHH-mm-ss.webm`（或 .mp4，取决于浏览器支持）

## 技术特性

### 支持的格式

- **首选格式**：WebM（VP8/VP9 编码），大多数现代浏览器支持
- **备选格式**：MP4，提供更好的兼容性

### 录制内容

- **视频**：
  - 本地摄像头全屏显示（仅本地时）
  - 远程参与者并排显示（多人会议时）
  - 本地摄像头显示在右下角（多人会议时）

- **音频**：
  - 本地麦克风音频
  - 所有远程参与者的音频
  - 系统音频（如果系统允许）

### 分辨率

- 录制画布：1280×720 (16:9)
- 帧率：30fps

## 浏览器兼容性

本地录制功能需要以下浏览器支持：

| 浏览器 | 支持版本 | 备注 |
|--------|---------|------|
| Chrome | 49+ | 原生支持 MediaRecorder |
| Firefox | 25+ | 原生支持 MediaRecorder |
| Safari | 14.1+ | 需要开启实验性特性 |
| Edge | 79+ | 原生支持 |

## 常见问题

### Q: 录制文件保存在哪里？
**A**: 文件会保存到浏览器的默认下载文件夹。通常是：
- Windows: `C:\Users\[用户名]\Downloads`
- macOS: `~/Downloads`
- Linux: `~/Downloads`

### Q: 能否修改录制文件名？
**A**: 当前版本使用时间戳自动生成文件名。如需自定义名称，可在下载后手动重命名。

### Q: 录制的文件太大，如何优化？
**A**: 
- 减少会议参与人数
- 降低画面清晰度（在浏览器设置中调整）
- 使用 MP4 格式可能获得更好的压缩率

### Q: 离开会议时录制没有停止，会发生什么？
**A**: 系统会自动停止录制并下载文件。

### Q: 支持音视频分离录制吗？
**A**: 当前版本只支持音视频一起录制到单个文件中。

## 常见问题排查

### 问题：点击"开始录制"后无反应
**解决**：
1. 确保已加入会议
2. 检查浏览器是否支持 MediaRecorder API
3. 查看浏览器控制台是否有错误信息（F12）

### 问题：只有视频没有音频
**解决**：
1. 检查麦克风是否已启用
2. 确保浏览器有访问音频设备的权限
3. 检查其他应用是否占用音频设备

### 问题：录制文件无法播放
**解决**：
1. 使用 VLC Player 或其他支持 WebM/MP4 的播放器
2. 尝试将文件转换为其他格式（使用 FFmpeg）
3. 检查文件是否完整（大小是否合理）

## 技术实现

本地录制基于以下技术：

- **MediaRecorder API**：提供音视频录制功能
- **Canvas API**：合成多个视频流到单个画布
- **Web Audio API**：处理音频源

相关文件：
- `/frontend/scripts/recorder.js` - 录制核心逻辑
- `/frontend/scripts/main.js` - UI 交互处理
- `/frontend/styles/main.css` - 录制按钮样式
